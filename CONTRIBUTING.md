# contributing

## commits

- format: `feat:`, `fix:`, `chore:`, `docs:`, … then **lowercase** description

## scope

- avoid editing `sdks/zep-python` unless you are intentionally upgrading the sdk
- prefer changes in `backend/`, `frontend/`, `examples/`, and compose/docs
